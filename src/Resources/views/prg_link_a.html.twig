{% if not only_open_tag %}
    <a class="prg-link {{ prg_link_class }}" data-submit="{{ prg_link_data|raw }}" data-target="{{ prg_link_target|raw }}">{{ prg_link_title|raw }}</a>
{% else %}
    <a class="prg-link {{ prg_link_class }}" data-submit="{{ prg_link_data|raw }}" data-target="{{ prg_link_target|raw }}">
{% endif %}