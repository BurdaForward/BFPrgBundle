{% if not only_open_tag %}
    <button class="prg-link {{ prg_link_class }}" data-submit="{{ prg_link_data|raw }}" data-target="{{ prg_link_target|raw }}">{{ prg_link_title|raw }}</button>
{% else %}
    <button class="prg-link {{ prg_link_class }}" data-submit="{{ prg_link_data|raw }}" data-target="{{ prg_link_target|raw }}">
{% endif %}